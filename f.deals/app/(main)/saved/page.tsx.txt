// app/(main)/saved/page.tsx
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/Button';
import { SignUpModal } from '@/components/ui/SignUpModal';
import { Heart, Plus } from 'lucide-react';

export default function SavedPage() {
  const [isModalOpen, setIsModalOpen] = useState(false);

  return (
    <>
      <SignUpModal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} />
      <div className="bg-white rounded-2xl shadow-lg p-6">
        <h2 className="text-2xl font-bold mb-6">Your Saved Searches</h2>
        {/* This is the empty state UI */}
        <div className="text-center py-16 border-2 border-dashed border-gray-300 rounded-xl">
          <div className="text-gray-500 mb-4">
            <Heart className="mx-auto h-12 w-12" />
          </div>
          <h3 className="text-lg font-medium text-gray-700">No saved searches yet</h3>
          <p className="text-gray-500 mb-6">Create an account to save routes and track price drops.</p>
          <Button onClick={() => setIsModalOpen(true)}>
            <Plus className="mr-2 h-4 w-4" /> Create Account & Add Route
          </Button>
        </div>
      </div>
    </>
  );
}